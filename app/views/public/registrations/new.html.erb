<div class="container">
  <div class="row justify-content-center">
    <div class="col-6 text-center mt-5">
      <h4>新規登録</h4>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-6">
      <%= form_with model: @user, url: user_registration_path do |f| %>
        <table class="table table-borderless matsuri-style">
          <tr>
            <td><%= f.label :名前 %></td>
            <td><%= f.text_field :name, autofocus: true, autocomplete: "name", class: "form-control" %></td>
          </tr>
          <tr>
            <td><%= f.label :メールアドレス %></td>
            <td><%= f.email_field :email, autocomplete: "email", class: "form-control" %></td>
          </tr>
          <tr>
            <td><%= f.label :パスワード %></td>
            <td><%= f.password_field :password, autocomplete: "current-password", class: "form-control" %></td>
          </tr>
          <tr>
            <td></td>
            <td>
              <%= f.submit "Sign Up", class:"btn btn-success"  %>
            </td>
          </tr>
        </table>
      <% end %>
    </div>
  </div>
  <div class="row justify-content-center my-3">
    <div class="col-6 text-center">
      <h6>登録済みの方は</h6>
      <%= link_to "こちら", new_user_session_path, class: "underline" %>からログインしてください。
    </div>
  </div>
</div>